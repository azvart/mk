import BentoCard from "@/app/components/BentoCard";import Link from "next/link";import { fetchFromServer } from "@/lib/fetchFromServer";async function getUser(){    const response = await fetchFromServer('user', 'GET')    if(response.ok){        return await response.json();    }}export default async function UserWidgetPage(){    await getUser();    return <Link href='/user'><BentoCard content="Users" /></Link>}