import { ReactNode } from "react";import {    Breadcrumb,    BreadcrumbItem,    BreadcrumbLink,    BreadcrumbList,} from "@/components/ui/breadcrumb"import { Separator } from "@/components/ui/separator";import { SidebarProvider, SidebarInset, SidebarTrigger } from "@/components/ui/sidebar";type DashboardLayoutProps = {    children: ReactNode;    user: ReactNode;    transactions: ReactNode;    settings: ReactNode;    map: ReactNode}export default async function DashboardLayout({ children, user, transactions, settings, map }: DashboardLayoutProps){    return <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 max-w-5xl">                {children}                {user}                {transactions}                {settings}                {map}            </div>}