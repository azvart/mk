import { FC, ReactNode } from 'react';import { cn } from '@/lib/utils';import { Card, CardContent, CardHeader, CardFooter, CardTitle, CardDescription } from "@/components/ui/card";type BentoCardProps = {    title?:string    description?:string    content: string | ReactNode    footer?: string | ReactNode    cardClassName?:string    contentClassName?:string    footerClassName?:string}const BentoCard:FC<BentoCardProps> = ({title, description, content, footer, cardClassName, contentClassName, footerClassName}) => {    return <Card className={cn("p-6 rounded-2xl shadow-lg bg-white hove:shadow-2xl transition-all flex flex-col items-center", cardClassName)}>        {(title || description )&& <CardHeader>            {title && <CardTitle>{title}</CardTitle>}            {description && <CardDescription>{description}</CardDescription>}        </CardHeader>}        <CardContent className={cn('items-center flex h-auto w-auto',contentClassName)}>            {content}        </CardContent>        {footer && <CardFooter className={cn(footerClassName)}>            {footer}        </CardFooter>}    </Card>}export default BentoCard;